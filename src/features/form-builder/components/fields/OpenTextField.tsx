import { TOpenTextField } from "@/types/form-types"

export const OpenTextField: React.FC<TOpenTextField> = (props) => {
  return <div>OpenTextField Component</div>
}

export const OpenTextFieldDesigner: React.FC<TOpenTextField> = (props) => {
  return <div>OpenTextFieldDesigner Component</div>
}

export const OpenTextFieldProperties: React.FC<TOpenTextField> = (props) => {
  return <div>OpenTextFieldProperties Component</div>
}
